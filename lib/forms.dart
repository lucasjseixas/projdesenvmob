import 'package:flutter/material.dart';

class PaginaDois extends StatelessWidget {
  const PaginaDois({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pagina Dois'),
      ),
    );
  }
}